parameters:
  usuarioPostBody:
    in: body
    name: body
    description: Ejemplo de creación de una Empresa
    required: true
    schema:
      $ref: "#/definitions/empresaPostBody"
  usuarioPutBody:
    in: body
    name: body
    description: Ejemplo de actualización de una Empresa
    required: true
    schema:
      $ref: "#/definitions/empresaPutBody"

